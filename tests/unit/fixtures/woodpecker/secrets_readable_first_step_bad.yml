build:
  steps:
    - name: build
      image: python:3.11.9-slim
      commands:
        - pip install -r requirements.txt
    - name: deploy
      image: alpine:3.19
      secrets:
        - deploy_token
      commands:
        - ./deploy.sh
