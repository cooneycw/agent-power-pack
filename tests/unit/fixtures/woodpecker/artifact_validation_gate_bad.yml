build-and-push:
  steps:
    - name: build-image
      image: docker:27.1.1
      commands:
        - docker build -t myapp:${CI_COMMIT_SHA} .
    - name: push-image
      image: docker:27.1.1
      commands:
        - docker push myapp:${CI_COMMIT_SHA}
